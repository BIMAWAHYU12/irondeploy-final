name: Deploy Simple

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: self-hosted  # <--- Perintah sakti buat VPS kamu
    
    steps:
    - name: Ambil Kodingan
      uses: actions/checkout@v3

    - name: Login Docker Hub
      run: docker login -u ${{ secrets.DOCKER_USER }} -p ${{ secrets.DOCKER_PASS }}

    - name: Build & Push
      run: |
        # Ganti dengan nama image kamu
        IMAGE_TAG=${{ secrets.DOCKER_USER }}/irondeploy-final:${{ github.sha }}
        docker build -t $IMAGE_TAG .
        docker push $IMAGE_TAG

    - name: Deploy ke VPS
      run: |
        IMAGE_TAG=${{ secrets.DOCKER_USER }}/irondeploy-final:${{ github.sha }}
        
        # Hapus container lama (kalau ada)
        docker stop iron-app || true
        docker rm iron-app || true
        
        # Jalankan container baru (Tanpa monitoring)
        docker run -d \
          --name iron-app \
          -p 80:80 \
          --restart always \
          $IMAGE_TAG